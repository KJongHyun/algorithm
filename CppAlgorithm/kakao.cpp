/*
//
// Created by JongHyeon Kim on 2019/11/09.
//

#include <iostream>
#include <stack>
#include <vector>
using namespace std;
stack<int> s;
int burst_count = 0;
int solution(vector<vector<int>> board, vector<int> moves);
int main()
{
    s.push(1);
    cout << s.top() << endl;

    return 0;
}

vector<long long> solution(long long k, vector<long long> room_number) {
    int max = 0;
    vector<long long> answer;
    for (long i = 0; i < room_number.size(); i++) {
        long room = room_number[i];
        if (find(answer.begin(), answer.end(),room) != answer.end()) {

        }
    }
    return answer;
}*/
